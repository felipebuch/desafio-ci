steps:

- id: "Rodando testes"
  name: "gcr.io/cloud-builders/go"
  args: ["test","main"]
  env: ["GOPATH=."]

- id: "Compilando projeto"
  name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-f', 'Dockerfile.builder','-t', 'gcr.io/$PROJECT_ID/builder-desafio-ci:latest', '.']

- id: "Rodando container"
  name: 'gcr.io/cloud-builders/docker'
  args: ['run', '--name', 'container-desafio', 'gcr.io/$PROJECT_ID/builder-desafio-ci:latest']

- id: "Verificando docker ps"
  name: 'gcr.io/cloud-builders/docker'
  args: ['ps', '-a']

- id: "Copiando arquivo binario"
  name: 'gcr.io/cloud-builders/docker'
  args: ['container', 'cp', 'container-desafio:/go/src/desafio/desafio', 'desafio']

- id: "Build com arquivo binario"
  name: 'gcr.io/cloud-builders/docker'
  args: ['build','-f','Dockerfile.prod','-t','gcr.io/$PROJECT_ID/desafio-ci:latest','.']
